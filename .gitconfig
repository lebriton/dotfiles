[user]
	email = contact@bminusl.xyz
	name = Bryton Lacquement
[core]
	editor = vim
[alias]
	co = checkout
	ci = commit
	st = status
	who = shortlog -sne
	undo = reset --soft HEAD^
	unstage = reset HEAD
	amend = commit --amend
	dic = diff --cached
	oneline = log --pretty=oneline --abbrev-commit --graph --decorate
	br = branch
	di = diff
	sh = show
	rb = rebase
	rbi = rebase -i
	rbc = rebase --continue
	rba = rebase --abort
	fx = !sh -c 'git commit -m \"This fixes commit $(git rev-parse $1).\" --fixup \"$@\"' -
	dit = difftool --dir-diff
	wip = commit -am WIP --no-verify
[diff]
	tool = kompare
