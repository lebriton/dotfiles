# dotfiles

```
mkdir -p ~/workspace/{bin,projects,snippets}

curl -o- https://gitlab.com/lebriton/dotfiles/-/raw/main/.install.sh | bash -s ssh
wget -qO- https://gitlab.com/lebriton/dotfiles/-/raw/main/.install.sh | bash -s ssh
```
